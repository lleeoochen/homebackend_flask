# Home Backend Server

### Facts
- Server: Python Flask
- Gateway Interface: WSGI
- Proxy Interface: Nginx

### Setup Server (Local)
`./setup`

### Start Server (Local)
'./run'

### Start Server (Remote)
- `sudo systemctl start/restart homebackend`
